"""ข้อความคำอธิบาย (tooltip) ภาษาไทยของทั้งแอป — แยกเป็น dict ตามหน้าจอ
เพิ่ม/แก้ข้อความที่นี่ที่เดียว ไม่ต้องไล่หาในโค้ด UI"""

MAIN = {
    "run_mode_direct": "เลือก Loop จาก dropdown แล้วกด Start จะรันทันที ไม่ต้องรอ trigger บนจอ",
    "run_mode_trigger": "บอทจะเฝ้าหน้าจอหา trigger image ที่ตั้งไว้ใน States — เจอเมื่อไหร่ค่อยรัน loop ที่ผูกไว้อัตโนมัติ",
    "loop_combo": "เลือก Loop ที่จะรัน (ใช้ตอนโหมด 'เลือก Loop รันทันที')",
    "mode_copilot": "Copilot = เจอ trigger แล้วถามยืนยันก่อนรันทุกครั้ง เหมาะกับตอนยังไม่มั่นใจ 100%",
    "mode_agent": "Agent = เจอ trigger แล้วรันทันทีอัตโนมัติ ไม่ถามยืนยัน",
    "start": "เริ่มรันบอทตามโหมดที่เลือกไว้ด้านบน (นับถอยหลัง 3 วิ ให้สลับไปหน้าต่างปลายทางก่อนเริ่ม)",
    "pause": "พักการทำงานชั่วคราว กดอีกครั้ง (Resume) เพื่อทำงานต่อจากจุดเดิม",
    "stop": "หยุดบอททันที (เทียบเท่ากด ESC หรือลากเมาส์ไปมุมซ้ายบนจอ)",
    "capture_trigger": "จับภาพหน้าจอเก็บไว้เป็น Trigger — บอทจะเฝ้าหาภาพนี้แล้วรัน loop อัตโนมัติในโหมด 'เฝ้า Trigger'",
    "capture_element": "จับภาพหน้าจอเก็บไว้เป็น Element — ใช้เป็นเป้าหมายให้ step เช่น click_image ค้นหาบนจอ",
    "sequence_editor": "เปิดตัวแก้ไข Loop/Step — สร้างและจัดลำดับขั้นตอนที่บอทจะทำ",
    "schedule": "ตั้งเวลาให้บอทรัน loop อัตโนมัติผ่าน Windows Task Scheduler (รันแบบ headless ไม่ต้องเปิดแอปทิ้งไว้)",
    "observe": "โหมดสังเกต (Copilot): จดค่าที่คุณกรอกใน SAP ไว้ในเครื่องนี้เท่านั้น "
               "เมื่อพบว่ากรอกค่าเดิมซ้ำหลายครั้ง จะเสนอสร้าง loop ให้อัตโนมัติ — "
               "ไม่จดช่องรหัสผ่าน และจะถามยืนยันก่อนสร้างเสมอ (กดอีกครั้งเพื่อหยุดและดูข้อเสนอ)",
    "state_list": "รายการ State ที่ตั้งไว้ — แต่ละ state ผูก trigger image กับ loop ที่จะรันเมื่อเจอภาพนั้น (แก้ได้ใน Sequence Editor → States)",
}

SEQ_EDITOR = {
    "loop_list": "รายชื่อ Loop ทั้งหมด — คลิกเลือกเพื่อดู/แก้ไข step",
    "new_loop": "สร้าง Loop ใหม่ (ยังไม่มี step ต้องกด + Add Step เพิ่มเอง)",
    "loop_settings": "ตั้งค่า Loop นี้ — ผูก CSV ข้อมูล, กำหนดวิธีรับมือเมื่อแถวพลาด (stop/skip/recover)",
    "delete_loop": "ลบ Loop นี้ทิ้งถาวร (ลบแล้วกู้คืนไม่ได้ ต้องกดยืนยันอีกครั้ง)",
    "edit_global_variables": "แก้ตัวแปร Global — ใช้ได้ทุก loop ผ่าน {ชื่อตัวแปร}; เว้นค่าว่างไว้ = บอทจะถามตอนกด Start",
    "edit_loop_variables": "แก้ตัวแปรเฉพาะ Loop นี้ — ทับค่าตัวแปร Global ที่ชื่อซ้ำกัน เฉพาะตอนรัน loop นี้",
    "edit_states": "จัดการ State — ผูก trigger image แต่ละภาพเข้ากับ loop ที่จะรันเมื่อเจอภาพนั้น (ใช้ในโหมด 'เฝ้า Trigger')",
    "export_loop": "Export loop นี้เป็นไฟล์ .botpack (รวมรูปภาพที่ใช้) เพื่อแชร์ให้เครื่องอื่น",
    "import_loop": "Import loop จากไฟล์ .botpack ที่ export มา — เพิ่มเข้า config ปัจจุบัน (เปลี่ยนชื่อกันซ้ำอัตโนมัติ)",
    "open_csv": "เปิดไฟล์ข้อมูล (CSV/Excel) ที่ loop นี้ผูกไว้ ด้วยโปรแกรมเริ่มต้นของเครื่อง",
    "add_step": "เพิ่ม Step ใหม่ต่อจาก step ที่เลือกอยู่ (หรือต่อท้ายถ้าไม่ได้เลือก)",
    "edit_step": "แก้ไข step ที่เลือกอยู่ (หรือดับเบิลคลิกที่ step ในลิสต์ก็ได้)",
    "move_up": "ย้าย step ที่เลือกขึ้น 1 ตำแหน่ง",
    "move_down": "ย้าย step ที่เลือกลง 1 ตำแหน่ง",
    "delete_step": "ลบ step ที่เลือกทิ้ง (ต้องกดยืนยัน)",
    "record": "อัดการคลิก/พิมพ์ของคุณเป็น step อัตโนมัติ — เริ่มอัดหลัง 2 วิ กด F10 เพื่อหยุด แล้ว step จะถูกเพิ่มต่อท้าย loop",
    "save_config": "บันทึกการแก้ไขทั้งหมดลงไฟล์ config/bot_config.yaml",
}

STEP_DIALOG = {
    "action_type": "เลือกชนิด action ของ step นี้ — ฟอร์มด้านล่างจะเปลี่ยนตามชนิดที่เลือก",
    "browse": "เลือกไฟล์รูปที่มีอยู่แล้วจากเครื่อง",
    "capture_image": "จับภาพหน้าจอใหม่มาใช้เป็นรูปเป้าหมายของ step นี้",
    "preview_image": "เปิดดูรูปที่ช่องนี้ชี้ไปแบบเต็มๆ — เช็คว่าเป็นรูปที่ถูกต้องก่อนบันทึก",
    "pick_position": "เปิดรูปให้คลิกเลือกจุดที่จะกด (ถ้าไม่เลือก จะกดกลางรูปโดย default)",
    "csv_column_insert": "เลือกคอลัมน์จาก CSV ของ loop นี้ แล้วกด + เพื่อแทรก {csv.ชื่อคอลัมน์} ลงในช่องข้อความ",
    "variable_insert": "เลือกตัวแปร (🌐 Global หรือ 📍 เฉพาะ loop นี้) แล้วกด + เพื่อแทรก {ชื่อตัวแปร} ลงในช่องข้อความ",
    "region_pick": "ลากกรอบบนหน้าจอเพื่อเลือกพื้นที่อ่านข้อความด้วย OCR (x, y, กว้าง, สูง)",
    "inspect_element": "กดแล้วเอาเมาส์ไปชี้ element เป้าหมายบนจอ (มีนับถอยหลัง) ระบบจะอ่านคุณสมบัติของ element นั้นมาใส่ฟอร์มให้อัตโนมัติ",
    "sap_pick_field": "กดแล้วคลิก field ใน SAP ระหว่างนับถอยหลัง ระบบจะอ่าน Field ID มาใส่ให้อัตโนมัติ (ต้องเปิด SAP GUI Scripting)",
    "add_case": "เพิ่ม case ใหม่สำหรับ switch_image — ระบุรูปเป้าหมาย + step ที่จะทำเมื่อเจอรูปนั้น",
    "edit_case": "แก้ไข case ที่เลือกอยู่ (หรือดับเบิลคลิกในลิสต์)",
    "move_case_up": "ย้าย case ขึ้น 1 ตำแหน่ง (ลำดับมีผล — เช็กจากบนลงล่าง เจอก่อนใช้ก่อน)",
    "move_case_down": "ย้าย case ลง 1 ตำแหน่ง",
    "delete_case": "ลบ case ที่เลือกทิ้ง",
    "save": "บันทึก step/case/ค่าที่แก้ไข",
    "cancel": "ยกเลิก ไม่บันทึกการแก้ไข",
    "browse_save_path": "เลือกตำแหน่งไฟล์ปลายทางที่จะเขียนข้อมูลเพิ่ม (write_row)",
    "wait_before_decide": "รอรูปก่อนตัดสิน (วินาที): 0 = เช็คทันทีครั้งเดียว (default) / มากกว่า 0 = รอจนกว่าจะเจอหรือหมดเวลาค่อยตัดสิน — ใช้แก้ปัญหาหน้าต่างเด้งช้าแล้วหลุดไปกิ่งผิด",
    "call_loop_target": "เลือก Loop ที่จะเรียกเป็น subroutine — รัน step ทั้งหมดของ loop นั้นแล้วกลับมาทำ step ถัดไปในนี้ต่อ (ใช้แถว CSV ปัจจุบันร่วมกัน; ตัวแปรเฉพาะของ loop ที่ถูกเรียกถูกใช้ระหว่างรันแล้วคืนค่าเดิมเมื่อจบ; ไม่สน data_source/on_row_error/setup_steps ของ loop ที่ถูกเรียก — เรียกวนกลับมาที่ตัวเอง (recursion) จะ error)",
}

LOOP_SETTINGS = {
    "csv_data_source": "ไฟล์ CSV/Excel ที่ loop นี้จะวนอ่านทีละแถว — เว้นว่างถ้า loop นี้ไม่ต้องใช้ข้อมูลจากไฟล์",
    "browse_csv": "เลือกไฟล์ CSV/Excel จากเครื่อง",
    "on_row_error": "เลือกวิธีรับมือเมื่อบอททำแถวใดแถวหนึ่งไม่สำเร็จ: stop = หยุดทั้งงาน, skip = ข้ามแถวนั้นไปทำถัดไป, recover = พยายามกู้คืนอัตโนมัติแล้วลองใหม่",
    "error_log_path": "ไฟล์ที่จะบันทึกแถวที่พลาด (เลขแถว, เวลา, ข้อความ error) — สร้างให้อัตโนมัติเมื่อ policy เป็น recover",
    "browse_error_log": "เลือกตำแหน่งไฟล์บันทึก error",
    "recovery_steps": "Step ที่จะรันทันทีหลังเจอ error ก่อนลองแถวนั้นใหม่ (เช่น กด ESC, คลิกกลับหน้าแรกของ SAP) เพื่อให้บอทกลับสู่สภาพพร้อมทำงานต่อ",
    "setup_steps": "Step ที่จะรันครั้งเดียวก่อนเริ่มแถวแรก (เช่น login เข้าโปรแกรม) — ยังใช้ {csv.X} ไม่ได้เพราะยังไม่เริ่มแถว ถ้า step ไหนพลาด บอทจะหยุดทั้งงานทันที",
}

ERROR_DIALOG = {
    "recapture": "ปิดหน้าต่างนี้แล้วเปิดเครื่องมือจับภาพใหม่ เพื่อบันทึกรูปเป้าหมายทับรูปเดิม (ใช้เมื่อรูปเก่าหาไม่เจอเพราะหน้าจอเปลี่ยน) — จับเสร็จจะ retry step นี้ให้อัตโนมัติ",
    "retry": "ลอง step เดิมซ้ำอีกครั้ง โดยไม่เปลี่ยนแปลงอะไร",
    "inject_action": "เลือกชนิดคำสั่งที่จะแทรกก่อน retry: key = กดคีย์, click = คลิกพิกัด, wait = รอกี่วินาที",
    "inject_value": "ค่าของคำสั่งที่เลือก — key: ชื่อคีย์ (เช่น enter) · click: x,y (เช่น 100,200) · wait: จำนวนวินาที",
    "inject_add": "เพิ่มคำสั่งนี้เข้าคิว 'แทรกคำสั่งกู้ภัย' ด้านล่าง (ยังไม่รัน ต้องกด Inject & Retry ถึงจะรัน)",
    "inject_and_retry": "รันคำสั่งที่เพิ่มไว้ทั้งหมดตามลำดับ แล้วลอง step เดิมซ้ำอีกครั้ง",
    "skip_step": "ข้าม step นี้ไปเลย ไม่ทำ แล้วทำ step ถัดไปต่อ",
    "restart_row": "เริ่มทำแถว CSV ปัจจุบันใหม่ตั้งแต่ step แรกของแถวนั้น",
    "skip_row": "บันทึก error + screenshot ลง error log แล้วข้ามแถวนี้ไปทำแถวถัดไปเลย",
    "stop": "หยุดบอททั้งหมดทันที",
}

SCHEDULE = {
    "loop_select": "เลือก Loop ที่จะให้รันตามตารางเวลา",
    "frequency": "daily = รันทุกวันตามเวลาที่ตั้ง / once = รันครั้งเดียวตามวันที่+เวลาที่ระบุ",
    "time_field": "เวลาที่จะรัน รูปแบบ HH:MM (24 ชม.) เช่น 08:00",
    "date_field": "วันที่จะรัน (เฉพาะ once) รูปแบบ MM/DD/YYYY",
    "create": "สร้าง scheduled task ใน Windows Task Scheduler ให้รัน loop นี้แบบ headless ตามเวลาที่ตั้ง",
    "task_list": "รายการ task ที่ตั้งไว้แล้ว (เฉพาะที่สร้างจากแอปนี้ ขึ้นต้นด้วย AutoBot_)",
    "delete_task": "ลบ task ที่เลือกออกจาก Windows Task Scheduler ถาวร",
    "close": "ปิดหน้าต่างนี้",
}

SAP_COMPARE = {
    "cancel": "ปิดหน้าต่างนี้โดยไม่บันทึก loop ใหม่",
    "save": "สร้าง loop ใหม่จาก SAP script ที่จดได้ (ไม่ทับ loop เดิม) — ไปแก้ไขต่อได้ใน Sequence Editor",
}
